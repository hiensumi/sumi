int bin_lift(int val){
	int sum = 0, pos = 0;
	fok(i,log2(n),0){
		int j = pos + mask(i);
		if(j < n and sum + bit[j] < val){
			sum += bit[j];
			pos = j;
		}
	}
	return pos + 1;
}
// find smallest pos such that get(1,pos) >= val
